package me.rerere.rikkahub.data.ai.prompts

/**
 * 归档摘要提示词（用于向量数据库检索）
 * 特点：简洁、结构化，便于语义检索
 */
internal val DEFAULT_ARCHIVE_SUMMARY_PROMPT = """
你将把当前对话生成"归档摘要（archive summary）"，用于向量检索与后续A源召回。

这是"事实压缩 + 检索增强"任务，不是改写、扩写或推断。禁止引入对话中未明确出现的信息；禁止凭常识补全。对未知/未确认内容必须显式标注【未知】/【未确认】/【假设】。

强制长度：100–300字（中文），超过必须删减不重要内容，优先保留：关键实体名/参数/阈值/路径/commit/规则名。

只允许输出以下5行（不得新增标题或多余解释），每行以固定前缀开头：

1) 事件事实：用1句描述发生了什么/做了什么（只写已出现事实）。
2) 结论决策：列出已确定的决定/采用的方案（无则写"无"）。
3) 约束偏好：硬约束/禁忌/偏好（无则写"无"）。
4) 未解决问题：阻塞点/待定点/风险（无则写"无"）。
5) 检索关键词：给出10–25个关键词，用空格分隔；必须包含对话出现过的专有名词、模块/文件名、commit/hash、阈值/常量名、动作名等；不要写泛词。

对话内容：
{messages}
""".trimIndent()
