package me.rerere.rikkahub.data.ai.prompts

/**
 * 默认的上下文压缩（Running Summary）提示词
 *
 * 用于对当前对话进行"高保真压缩总结"，在不重新阅读完整对话的前提下，
 * 可靠地继续后续工作。
 *
 * 变量说明：
 * - {previous_archive_summary}: 上一窗口的归档摘要（可能为空）
 * - {messages}: 当前滑动窗口的对话内容（已格式化的 User/Assistant 列表）
 */
internal val DEFAULT_CONTEXT_COMPRESSION_PROMPT = """
你的任务是对当前对话进行"高保真压缩总结（compaction summary）"，用于在不重新阅读完整对话的前提下，可靠地继续后续工作。

这是一次"压缩与保真"任务，而不是重写、概括或合理补全。
禁止引入对话中未明确出现的信息。
禁止基于常识、经验或概率进行推断。
当信息不存在、未确认或仅为假设时，必须明确标注。

在给出最终总结之前，先在 <analysis> 标签中完成内部检查与组织，确保无遗漏、无臆测。你的分析必须遵循以下规则：

一、逐段时序分析
按时间顺序分析每一段用户与助手的交互（可将连续、同一主题的多轮对话合并为一个"段落"），对每一段明确识别：

- 用户的**明确请求、目标与意图**（包括意图的变化）
- 助手**实际做了什么**（已执行）与**仅提出但未执行的内容**（建议/计划）
- 明确做出的**决定、结论与理由**
- 所有**硬性约束**（必须/禁止）与**软性偏好**
- 用户给出的**反馈、修正、否定或行为指令**
- 出现的任何**具体对象或工件**（文件、代码、命令、配置、文档、链接、数据、图像、规则等）
- 继续工作所必需的**精确信息**（名称、参数、格式、路径、结构、标准等）
- 出现的**错误、失败、中断或偏差**，以及当时如何应对
- 尚未解决的问题、不确定点或风险

二、强去幻觉校验（必须执行）
在完成内容整理后，逐项检查：

- 所有事实是否都**可追溯到对话中的明确表述**
- 若某信息未被明确给定，必须使用以下之一进行标注：
  - 【未知】：对话中完全未提供
  - 【未确认】：被提及但未得到确认或验证
  - 【假设】：仅作为推测或可能性被讨论
- 禁止将【假设】【未确认】内容当作事实陈述
- 若存在多种可能但未被用户选定，必须并列列出，不得擅自收敛

如果对话中涉及代码、命令、配置或结构化内容：
- 仅在其对后续工作有必要时才包含
- 保留关键片段的**原始形式或接近原文的形式**
- 不得"修正""优化"或"补全"原内容

---

你的最终输出必须严格使用以下结构与标题：

<summary>

1. 主要请求与核心意图
   - 列出用户所有明确提出的请求与目标
   - 如意图发生变化，按时间顺序说明
   - 明确当前"最新、有效"的意图是什么

2. 约束与偏好（关键）
   - 硬性约束（必须 / 禁止）
   - 软性偏好（风格、粒度、形式、侧重点）
   - 所有来自用户的修正性反馈

3. 关键背景与领域上下文
   - 影响决策的重要背景信息
   - 相关领域概念、规则、方法或工作流
   - 若某背景仅为假设或推测，必须标注

4. 工件与具体对象
   - 列出所有被提及、查看、创建或修改的对象
   - 对每一项说明：
     - 名称 / 标识
     - 在当前工作的作用
     - 已发生的变化（如有）
     - 对继续工作不可或缺的关键内容（必要时原文摘录）
   - 若没有任何工件，明确写明：未涉及具体工件

5. 已执行的行动（事实层）
   - 明确列出助手**已经完成**的行为与结果
   - 与"仅建议但未执行"的内容严格区分

6. 错误、问题与修复
   - 所有出现过的错误、失败或阻塞
   - 当时的处理方式与结果
   - 用户针对错误给出的明确反馈（如有）

7. 决策与问题解决过程
   - 已做出的关键决策及理由
   - 被否定或搁置的方案（如有）及原因

8. 全部用户消息（高保真）
   - 按时间顺序列出**所有用户消息**（不含工具输出）
   - 尽量保持原措辞
   - 若压缩，仅允许删除无关寒暄，不得删除约束、指令或否定

9. 未完成任务与开放问题
   - 用户明确要求但尚未完成的事项
   - 当前仍需澄清或决策的问题

10. 当前工作状态（最近上下文）
    - 总结在本次压缩发生前，工作进行到哪一步
    - 明确最后一条用户指令与助手所处状态

11. 可选的下一步
    - 仅在"下一步与最近一次用户明确请求直接相关"时才填写
    - 必须引用最近对话中的原话，说明工作暂停在何处
    - 禁止引入新方向或旧已完成任务

</summary>

如果对话上下文中存在额外的总结指令（例如"聚焦某类内容""必须逐字保留某部分"），你必须在不删除任何上述结构的前提下，严格执行这些指令。

{previous_archive_summary}

本轮新增对话内容：
{messages}

请基于以上信息生成新的压缩总结：
""".trimIndent()
